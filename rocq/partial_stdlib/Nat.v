Require Export mappings.

(* The direct translated Nat proof file does not currently check against
   native Rocq nat. The checked Nat interface used by this slice lives in
   mappings.v, where the Lambdapi Nat symbols are mapped to native Rocq
   definitions and proved shim lemmas. *)
